# Coalition Server - Backend Documentation

Серверная часть приложения для управления заявками коалиции.

## 🛠️ Технологический стек

- **Express.js** - веб-фреймворк
- **MongoDB** - база данных
- **JWT** - аутентификация
- **bcryptjs** - хеширование паролей
- **Passport.js** - готовность для OAuth

## 🚀 Установка и запуск

### 1. Установка зависимостей:
```bash
npm install
```

### 2. Настройка переменных окружения:
```bash
cp .env.example .env
```

Отредактируйте `.env`:
```env
PORT=5000
MONGODB_URI=mongodb://localhost:27017/coalition
JWT_SECRET=your_super_secret_key_here_change_me
FRONTEND_URL=http://localhost:3000
```

### 3. Убедитесь, что MongoDB запущен:
```bash
# На Windows (если установлен как сервис)
# MongoDB должна работать автоматически

# Или запустите локально:
mongod
```

### 4. Запуск сервера:
```bash
# Продакшн
npm start

# Разработка (с автоперезагрузкой)
npm run dev
```

Сервер будет доступен на `http://localhost:5000`

## 📚 API Документация

### Authentication endpoints

#### POST `/api/auth/register`
Регистрация нового пользователя
```json
{
  "username": "your_username",
  "email": "your@email.com",
  "password": "password123"
}
```
**Ответ:**
```json
{
  "message": "User registered successfully",
  "token": "eyJhbGc...",
  "user": {
    "id": "123...",
    "username": "your_username",
    "email": "your@email.com",
    "role": "user"
  }
}
```

#### POST `/api/auth/login`
Вход пользователя
```json
{
  "email": "your@email.com",
  "password": "password123"
}
```

#### GET `/api/auth/me`
Получить информацию о текущем пользователе
**Требуется:** Bearer токен в заголовке `Authorization`

---

### Questions endpoints

#### GET `/api/questions`
Получить все активные вопросы (доступно всем)
**Ответ:**
```json
[
  {
    "_id": "123...",
    "title": "Вопрос 1",
    "description": "Описание вопроса",
    "createdBy": {
      "_id": "admin_id",
      "username": "admin"
    },
    "isActive": true,
    "createdAt": "2026-02-12T10:00:00Z"
  }
]
```

#### GET `/api/questions/:id`
Получить вопрос со всеми ответами (только для админов)

#### POST `/api/questions`
Создать новый вопрос (только админ)
```json
{
  "title": "Заголовок вопроса",
  "description": "Полное описание вопроса"
}
```
**Требуется:** Admin роль

#### PUT `/api/questions/:id`
Обновить вопрос (только админ)

#### DELETE `/api/questions/:id`
Удалить вопрос (только админ)

---

### Answers endpoints

#### POST `/api/answers`
Отправить ответ на вопрос
```json
{
  "questionId": "623...",
  "answer": "Мой ответ на вопрос"
}
```

#### GET `/api/answers/user/my-answers`
Получить все ответы текущего пользователя

#### GET `/api/answers/question/:questionId`
Получить ответ пользователя на конкретный вопрос

---

## 👥 Модели данных

### User
```javascript
{
  _id: ObjectId,
  username: String (unique, required),
  email: String (unique, required),
  password: String (hashed),
  discordId: String (optional),
  discordUsername: String,
  discordAvatar: String,
  role: 'user' | 'admin',
  status: 'pending' | 'approved' | 'rejected',
  createdAt: Date,
  updatedAt: Date
}
```

### Question
```javascript
{
  _id: ObjectId,
  title: String (required),
  description: String (required),
  createdBy: ObjectId (ref: User),
  isActive: Boolean,
  createdAt: Date,
  updatedAt: Date
}
```

### Answer
```javascript
{
  _id: ObjectId,
  questionId: ObjectId (ref: Question, required),
  userId: ObjectId (ref: User, required),
  answer: String (required),
  createdAt: Date,
  updatedAt: Date
}
```

## 🔒 Безопасность

- JWT токены с истечением (7 дней)
- Хеширование паролей через bcryptjs (10 раундов)
- CORS настройка для фронтенда
- Проверка прав доступа (Admin middleware)
- HTTP-only cookies для сессий (рекомендуется)

## 📝 Обработка ошибок

Все ошибки возвращают JSON с сообщением:
```json
{
  "message": "Описание ошибки"
}
```

HTTP статус коды:
- `200` - Успешный запрос
- `201` - Успешное создание
- `400` - Ошибка валидации
- `401` - Не авторизован
- `403` - Доступ запрещен
- `404` - Не найдено
- `500` - Ошибка сервера

## 🔄 Дополнительные интеграции

### Discord OAuth (готово к интеграции)
```javascript
// Уже установлены необходимые пакеты:
// passport-discord, passport

// Требуется добавить:
// - Discord Client ID/Secret
// - Маршрут для Discord callback
// - Strategy конфигурация
```

## 🐛 Отладка

Включить детальное логирование:
```javascript
// В index.js
app.use((req, res, next) => {
  console.log(`${req.method} ${req.path}`, req.body);
  next();
});
```

## 📦 Зависимости

- express - веб-фреймворк
- mongoose - MongoDB драйвер
- jsonwebtoken - JWT
- bcryptjs - хеширование
- cors - CORS middleware
- dotenv - переменные окружения

## 🚀 Деплой

Для деплоя на Heroku/Railway:
1. Добавьте процесс: `npm start`
2. Установите `MONGODB_URI` для облачной БД
3. Установите безопасный `JWT_SECRET`
4. Например: `heroku create`, `git push heroku main`

---

**Готово к использованию! Удачи с развитием сервера! 🎮**
