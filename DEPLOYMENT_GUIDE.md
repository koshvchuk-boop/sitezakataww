# Как поделиться сайтом с друзьями

## Способ 1: ngrok (быстро и просто) ⚡

### Установка ngrok:
```powershell
# Если не установлен
choco install ngrok

# Или скачай с ngrok.com и добавь в PATH
```

### Запуск:

**Терминал 1** - Фронтенд:
```powershell
cd c:\Users\kosti\Desktop\sitezakataw\client
npm run dev
```

**Терминал 2** - Бэкенд:
```powershell
cd c:\Users\kosti\Desktop\sitezakataw\server
npm start
```

**Терминал 3** - ngrok для фронтенда:
```powershell
ngrok http 3000
```

### Результат:
ngrok выдаст URL типа: `https://abc123-xyz.ngrok-free.dev`

Это и есть публичная ссылка! Друзья могут зайти сразу.

---

## Способ 2: Публичное развертывание

### Вариант A: Vercel (Фронтенд) + Railway (Бэкенд)

**Фронтенд на Vercel:**
1. Создай аккаунт на vercel.com
2. Импортируй репозиторий или загрузи папку client
3. Vercel автоматически разворачивает на публичный URL

**Бэкенд на Railway:**
1. Создай аккаунт на railway.app
2. Подключи MongoDB Atlas (уже используешь)
3. Разверни Node.js сервер
4. Аппендикуларуй DISCORD_CALLBACK_URL и FRONTEND_URL на Railway

---

## Способ 3: Локальная сеть (если друзья в одной сети)

Просто дай им IP адрес твоего компьютера + порт:
```
http://192.168.x.x:3000
```

Найти свой IP:
```powershell
ipconfig
# Ищи "IPv4 Address" в секции "Ethernet"
```

---

## Рекомендация:

🎯 **Для быстрого тестирования:** используй **ngrok** (Способ 1)
🚀 **Для долгосрочного:** развертывание на **Vercel + Railway** (Способ 2)
