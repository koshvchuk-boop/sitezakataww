# 🚀 БЫСТРЫЙ СТАРТ

## Автоматический запуск (рекомендуется)

### На Windows:
1. Откройте этот файл двойным кликом (запустит оба сервера)
   ```bash
   start-servers.bat
   ```

### На Mac/Linux:
```bash
chmod +x start-servers.sh
./start-servers.sh
```

---

## ⚙️ Ручной запуск

### Требования:
- Node.js v16+ установлен
- MongoDB запущена

### Терминал 1 - Сервер (API):
```bash
cd server
npm install  # Первый раз
npm run dev
```
✅ Сервер запустится на **http://localhost:5000**

### Терминал 2 - Клиент (Интерфейс):
```bash
cd client
npm install  # Первый раз
npm run dev
```
✅ Браузер откроет **http://localhost:3000**

---

## 🔧 Первая настройка

### 1. Переименуйте .env файл:

**Сервер (server/):**
```bash
cp .env.example .env
```

### 2. Проверьте MongoDB

**Windows:**
```bash
# Проверьте что сервис запущен:
net start MongoDB
```

**Если нет MongoDB:**
- Установите с https://www.mongodb.com/try/download/community
- **ИЛИ** используйте облако MongoDB Atlas (https://www.mongodb.com/cloud/atlas)
  - Создайте бесплатный кластер
  - Скопируйте строку подключения в `server/.env`

### 3. Создайте первого админа

1. После запуска - перейдите в MongoDB Compass или терминал
2. Создайте нового пользователя с `role: "admin"`:

```javascript
db.users.insertOne({
  username: "admin",
  email: "admin@example.com",
  password: "$2a$10$...",  // bcrypt хеш пароля
  role: "admin",
  status: "approved"
})
```

**Или используйте GUI:**
- MongoDB Compass → Connect → Open MongoDB Shell
- Вставьте commands выше

---

## 🎯 Первая сессия

1. **Главная страница:** http://localhost:3000
2. Нажмите **"Регистрация"**
3. Создайте аккаунт
4. Нажмите **"Как попасть в коалицию"** → ответьте на вопросы
5. На другом браузере войдите как **админ** → перейдите в **"⚙️ Панель админа"**
6. Создавайте вопросы и смотрите ответы! 🎉

---

## 📱 Тестовые данные (опционально)

Загрузить демо-вопросы:

```bash
# В MongoDB Shell:
db.questions.insertMany([
  {
    title: "Почему вы хотите присоединиться?",
    description: "Расскажите нам немного о себе и почему вас интересует наша коалиция",
    createdBy: ObjectId("admin_id_здесь"),
    isActive: true
  },
  {
    title: "Какой ваш опыт в Minecraft?",
    description: "Укажите, как долго вы играете и в каких режимах",
    createdBy: ObjectId("admin_id_здесь"),
    isActive: true
  }
])
```

---

## 🆘 Если что-то не работает

### Ошибка: "Cannot connect to MongoDB"
```bash
# Убедитесь MongoDB запущена:
# Windows: net start MongoDB
# Mac: brew services start mongodb-community
# Linux: sudo systemctl start mongod
```

### Ошибка: "Port 5000 already in use"
```bash
# Измените PORT в server/.env на 5001
# И обновите URL в client/vite.config.js
```

### Ошибка: "Cannot find module"
```bash
# Заново установите зависимости:
npm install
```

### Клиент не видит сервер
```bash
# Проверьте что оба запущены:
# Сервер: http://localhost:5000/api/health (должен ответить)
# Клиент: http://localhost:3000
```

---

## 📚 Документация

- [Full README](README.md) - Полное описание
- [Server API Docs](server/README.md) - API методы
- [Client Documentation](client/README.md) - Frontend описание

---

## 🎮 Следующие шаги

1. **Добавьте свои вопросы** в админ-панели
2. **Кастомизируйте стиль** - отредактируйте цвета в `client/src/styles/globals.css`
3. **Добавьте Discord OAuth** - см. `server/routes/auth.js`
4. **Деплойте!** - на Heroku, Railway или другом хостинге

---

## 💡 Советы

🔒 **Перед продакшном:**
- Измените `JWT_SECRET` на что-то очень сложное
- Установите HTTPS
- Используйте SSL сертификаты
- Включите CORS только для вашего домена

📊 **Мониторинг:**
- Смотрите логи сервера в терминале
- Используйте MongoDB Compass для просмотра БД
- Проверяйте DevTools браузера (F12)

🚀 **Масштабирование:**
- Перейдите на Redis для сессий
- Добавьте кэширование
- Оптимизируйте запросы к БД

---

**Готово! Наслаждайтесь разработкой! 🎉**

Вопросы? Проверьте логи в терминале или откройте DevTools (F12) в браузере.
