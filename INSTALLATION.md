# 📥 ПОШАГОВАЯ УСТАНОВКА & ЗАПУСК

## 🎯 Что нужно сделать ВСО СРАЗУ

> Если вы в спешке, выполните только это:

1. ✅ **Установите Node.js** (если нет): https://nodejs.org (v16+)
2. ✅ **Откройте терминал в папке проекта**
3. ✅ **Запустите скрипт:**
   - **Windows:** Двойной клик на `start-servers.bat`
   - **Mac/Linux:** `chmod +x start-servers.sh && ./start-servers.sh`
4. ✅ **Откройте браузер:** `http://localhost:3000`
5. ✅ **Готово!** 🎉

---

# 📋 ДЕТАЛЬНАЯ УСТАНОВКА (шаг за шагом)

## ШАГ 1: Проверка требований

### Windows, Mac или Linux - откройте терминал/PowerShell:

```bash
# Проверка Node.js (должен быть v16+)
node -v

# Проверка npm
npm -v
```

**Если ошибка:**
→ Установите Node.js с https://nodejs.org

---

## ШАГ 2: Настройка базы данных

### Выберите один вариант:

#### 🟦 Локальная MongoDB (проще для разработки)
```bash
# Windows - скачайте и установите:
# https://www.mongodb.com/try/download/community

# Запуск после установки:
net start MongoDB

# Проверка:
mongosh
```

#### ☁️ MongoDB Atlas (облачная)
1. Перейдите: https://www.mongodb.com/cloud/atlas
2. Создайте **Free Tier** кластер
3. Получите строку подключения
4. Вставьте в `server/.env`

**Подробнее:** Смотрите [DATABASE_SETUP.md](DATABASE_SETUP.md)

---

## ШАГ 3: Настройка переменных окружения

### Переименуйте .env файл:

```bash
cd server
cp .env.example .env  # На Mac/Linux
# Или вручную скопируйте .env.example в .env на Windows
```

### Отредактируйте `server/.env`:

```env
PORT=5000
# Если локальная MongoDB:
MONGODB_URI=mongodb://localhost:27017/coalition

# Если MongoDB Atlas (скопируйте из Atlas):
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/coalition?retryWrites=true&w=majority

JWT_SECRET=your_super_secret_key_min_32_chars_12345678901234567890
FRONTEND_URL=http://localhost:3000
```

---

## ШАГ 4: Установка зависимостей

### Сервер (Terminal 1):
```bash
cd server
npm install
```
Это может занять 2-3 минуты ☕

### Клиент (Terminal 2):
```bash
cd client
npm install
```

---

## ШАГ 5: Запуск сервера

### Terminal 1 (оставьте открытым):
```bash
cd server
npm run dev
```

**Результат:**
```
✅ Server running on http://localhost:5000
✅ MongoDB connected
```

---

## ШАГ 6: Запуск клиента

### Terminal 2 (оставьте открытым):
```bash
cd client
npm run dev
```

**Результат:**
```
✅ Local:   http://localhost:3000
✅ press h to show help
```

---

## ШАГ 7: Откройте браузер

### Перейдите на:
```
http://localhost:3000
```

**Должны увидеть:**
- ⚡ Красивый неон-интерфейс
- 🎨 Синий-фиолетовый градиент
- 📱 Кнопки входа/регистрации

---

## ШАГ 8: Создание первого аккаунта

1. Нажмите **"Регистрация"**
2. Заполните форму:
   - Имя: `testuser`
   - Email: `test@example.com`
   - Пароль: `password123`
3. Нажмите **"Зарегистрироваться"**
4. ✅ Вы должны войти автоматически

---

## ШАГ 9: Трансформация в администратора

### Способ 1: MongoDB Compass (Легче)

1. Скачайте: https://www.mongodb.com/products/compass
2. Установите и откройте
3. Нажмите **"New Connection"** → **"Connect"**
4. Слева найдите: `coalition` → `users`
5. Найдите вашего пользователя
6. Нажмите **Edit** и измените:
   ```javascript
   "role": "admin"  // Было: "user"
   ```
7. Обновите 

### Способ 2: Терминал (Для знающих)

```bash
mongosh

use coalition

db.users.updateOne(
  { username: "testuser" },
  { $set: { role: "admin" } }
)

# Проверка:
db.users.findOne({ username: "testuser" })
```

---

## ШАГ 10: Вход как администратор

1. Выйдите из текущего аккаунта (нопка выхода)
2. Вернитесь на http://localhost:3000/login
3. Введите свои данные (test@example.com / password123)
4. Вы должны увидеть в навигации **"⚙️ Панель админа"**

---

## ШАГ 11: Создание первого вопроса (АДМИН)

1. Нажмите **"⚙️ Панель админа"**
2. В левой колонке:
   - **Заголовок:** "Почему вы хотите вступить?"
   - **Описание:** "Расскажите немного о себе"
3. Нажмите **"Создать вопрос"**
4. ✅ Вопрос должен появиться в списке

---

## ШАГ 12: Проверка (АДМИН)

1. Откройте новый **Incognito окно браузера** (Ctrl+Shift+N)
2. Перейдите на http://localhost:3000
3. Создайте **новый аккаунт** (другое имя)
4. Нажмите **"🤝 Как попасть в коалицию"**
5. Ответьте на вопрос
6. **Вернитесь в админ-окно** и обновите страницу
7. Нажмите **👁️ (просмотр)** на вопросе
8. ✅ Должны увидеть ответ пользователя!

---

# 🎉 ВЫ ГОТОВЫ!

Поздравляем! Ваш сайт запущен и работает! 🚀

## 📝 Следующие шаги:

1. **Кастомизация:**
   - Измените цвета в `client/src/Styles/globals.css`
   - Отредактируйте текст на главной странице
   - Добавьте свои вопросы

2. **Чаты Discord/Telegram:**
   - Замените ссылки на кнопках в `Navigation.jsx`

3. **YouTube ссылка:**
   - Найдите ссылку в `Navigation.jsx` и обновите

4. **Добавьте больше админов:**
   - Повторите шаги 8-9 для других пользователей и изменяйте роль на "admin"

---

# 🆘 ЕСЛИ ЧТО-ТО НЕ РАБОТАЕТ

## **MongoDB не запускается**
```bash
# Windows
net start MongoDB

# Mac (если установлена через Homebrew)
brew services start mongodb-community

# Linux
sudo systemctl start mongod

# Docker (если используете)
docker start coalition-mongo
```

## **Порт 5000 занят**
```bash
# Измените PORT в server/.env на 5001
# Обновите клиент для подключения к new port
```

## **Ошибка "Cannot find module"**
```bash
# Заново установите
npm install

# Если не помогло:
rm -rf node_modules package-lock.json  # Удалите
npm install  # Переустановите
```

## **Клиент видит ошибку подключения**
```bash
# Проверьте что сервер запущен:
curl http://localhost:5000/api/health

# Если ошибка - перезагрузите сервер
# Нажмите Ctrl+C в окне сервера и запустите заново
```

## **"CORS Error" или подобные ошибки**
```bash
# Проверьте что в server/index.js стоит:
FRONTEND_URL=http://localhost:3000

# И в client/.env должен быть или готовый proxy в vite.config.js
```

---

# 📚 Дополнительная документация

- [README.md](README.md) - Полное описание проекта
- [DATABASE_SETUP.md](DATABASE_SETUP.md) - Подробно о БД
- [GETTING_STARTED.md](GETTING_STARTED.md) - Быстрый старт
- [server/README.md](server/README.md) - API документация
- [client/README.md](client/README.md) - Frontend документация

---

# 🚀 ДЕПЛОЙ В ИНТЕРНЕТ

После того как все работает локально, вы можете задеплоить:

### Вариант 1: Vercel + Railway (Рекомендуется)

**Frontend на Vercel:**
```bash
npm i -g vercel
cd client
vercel
```

**Backend на Railway:**
1. Перейдите https://railway.app
2. Создайте новый проект
3. Подключите гит-репозиторий
4. Установите MongoDB Atlas как БД
5. Deploy!

### Вариант 2: Heroku (Старый, но простой)

```bash
npm install -g heroku
heroku login
heroku create your-coalition-app
git push heroku main
```

---

**✅ Спасибо за использование Coalition! Успехов!** 🎮⚡
